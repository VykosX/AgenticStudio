pwd, shutdown, ls, ls -la, ls -R, locate, which, realpath, cat, head, tail, less, more, stat, file, wc -l, grep, rg, ag, sed, awk, sort, uniq, cut, tr, diff, patch, cp, cp -r, mv, mv -i, rm -i, mkdir, touch, ln -s, chmod, chown, du -sh, ps aux, top, htop, jobs, fg, bg, curl -I, curl -v, curl -X, wget --spider, wget -r, wget -c, ping -c 1, git status, git diff, git log, git show, uname -a, df -h, free -m, uptime, whoami, id, hostname, date, who, last, npm list, npm list astro, npm install, npm uninstall, npm run, npm start, npm test, yarn, pnpm, npm build, git commit, git add, git revert, git reset, git rebase, git checkout
